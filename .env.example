# GitHub OAuth Configuration
GITHUB_CLIENT_ID=Ov23liU6xcSFivLXndvB
GITHUB_CLIENT_SECRET=454fcf9f90ca6243f140ab9d9a0da4e72e08cf18
OAUTH_REDIRECT_URI=http://localhost:9002/auth/callback
OAUTH_STATE_SECRET=your-secret-key-change-me-in-production

# Admin Authentication - Username-based (REQUIRED)
ADMIN_INITIAL_USERNAME=pablogsal

# Legacy team-based auth (deprecated but kept for fallback)
ADMIN_GITHUB_ORG=python
ADMIN_GITHUB_TEAMS=memory-python-org

# Optional configurations
# Database Configuration (defaults handled by docker-compose)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost/memory_tracker

# CORS Configuration (defaults handled by docker-compose)
# CORS_ORIGINS=http://localhost:3000,http://localhost:9002